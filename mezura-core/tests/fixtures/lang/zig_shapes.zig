// mezura-expect lines=7 code=4 comments=2 extra=1 structs=1 enums=1
const s = "// not a comment";

// a comment
const Point = struct { x: i32 };
const Colour = enum { red };
var n: i32 = 1;  // trailing
