// mezura-expect lines=12 code=6 comments=3 extra=3 classes=1 interfaces=1 enums=1
/* a block
   comment */
const a = "// not a comment";
const b = 'also';
const c = `a template`;

interface Props { }
enum Colour { Red }

class Widget {
}
