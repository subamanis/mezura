# mezura-expect lines=15 code=9 comments=2 extra=4 types=1 interfaces=1 enums=1
type Query {
  greeting: String
}

interface Node {
  id: ID
}

# an enum
enum Color { RED, GREEN }
scalar Note @spec(url: "# not a comment")
"""
a description
"""
