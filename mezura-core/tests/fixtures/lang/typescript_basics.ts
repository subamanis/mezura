// mezura-expect lines=11 code=5 comments=3 extra=3 classes=1 enums=1
const s = "// not a comment";
const t = 'also';
const u = `a template`;

/* a block
   comment */
enum Colour { Red }

class Box {
}
