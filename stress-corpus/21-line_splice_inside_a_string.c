// mezura-real  7 lines 4 code 3 comment
// mezura-count 7 lines 4 code 3 comment
// trap: a backslash at the end of the line continues the string in C, so the // inside it is not a comment
char *s = "line one\
	// not a real comment\
	line three";
int x = 1;
