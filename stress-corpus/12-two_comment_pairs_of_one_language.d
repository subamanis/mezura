// mezura-real  6 lines 1 code 5 comment
// mezura-count 6 lines 1 code 5 comment
// trap: each pair ends only at its own closer, the other pair's token inside it is text
/* a plain block holding a +/ token as text */
/+ a nesting block holding a */ token as text +/
int x = 1;
