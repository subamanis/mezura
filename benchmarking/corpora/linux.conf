# What gets counted. Only the parts that differ between one tree and another live here;
# the flags that make the three tools do equal work are the benchmark's own and are in
# benchmark.py.
#
# languages   what mezura and scc are told to count, as extensions
# types       the same set as tokei spells it, which is language names and not extensions
# commit      blank means whatever the checkout is on, and the run is marked unpinned

name      = linux
remote    = https://github.com/torvalds/linux.git
commit    = 0ff41df1cb268fc69e703a08a57ee14ae967d0ca
languages = c,h,s,py,pl,rs,sh
types     = C,C Header,GNU Style Assembly,Python,Perl,Rust,Shell
