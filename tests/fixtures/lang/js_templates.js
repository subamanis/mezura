// mezura-expect lines=7 code=5 comments=2 classes=1
const url = `http://example.com/path`;
const block = `
  a line inside a template literal
`;
// a real comment, outside every template
class Templated {}
