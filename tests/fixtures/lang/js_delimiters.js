// mezura-expect lines=19 code=11 comments=2 extra=6 classes=1
const config = {
    items: [
        "one",
        "two"],
};

/* a block comment */
run(function () {
    done();
});

class Thing {
    go() {}
}
const nested = {a: {b: 1
}}
const map = {k:
    "v"}
