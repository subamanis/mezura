# mezura-expect lines=11 code=6 comments=2 classes=1
"""Module docstring with a " quote inside."""

import os

class Widget:
    '''Class docstring with a ' quote inside.'''

    def render(self):
        # a real comment, after both docstrings closed
        return os.sep
